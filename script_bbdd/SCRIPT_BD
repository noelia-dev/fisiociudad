CREATE TABLE `app_nco`.`user` (
  `id_user` INT NOT NULL,
  `password` VARCHAR(45) NULL,
  `correo` VARCHAR(45) NULL,
  `telefono` VARCHAR(45) NULL,
  `nombre` VARCHAR(45) NULL,
  `apellidos` VARCHAR(45) NULL,
  `es_admin` BOOLEAN NULL,
  PRIMARY KEY (`id_user`))
ENGINE = InnoDB;